services:
    php:
        build:
            args:
                PHP_IMAGE: ghcr.io/sylius/sylius-php:8.3-fixuid-xdebug-alpine
                PHP_RUNTIME_USER: "sylius:sylius"
        user: ${DOCKER_USER:-1000:1000}
        depends_on:
            mysql:
                condition: service_healthy
        environment:
            # You can move these environment variables to your .env.local file
            APP_ENV: ${ENV:-prod}
            APP_SECRET: EDITME
            DATABASE_URL: "mysql://root@mysql/sylius_%kernel.environment%"
            MAILER_DSN: smtp://mailhog:1025
            MESSENGER_TRANSPORT_DSN: doctrine://default
            SYLIUS_MESSENGER_TRANSPORT_MAIN_DSN: doctrine://default
            SYLIUS_MESSENGER_TRANSPORT_MAIN_FAILED_DSN: doctrine://default?queue_name=main_failed
            SYLIUS_MESSENGER_TRANSPORT_CATALOG_PROMOTION_REMOVAL_DSN: doctrine://default?queue_name=catalog_promotion_removal
            SYLIUS_MESSENGER_TRANSPORT_CATALOG_PROMOTION_REMOVAL_FAILED_DSN: doctrine://default?queue_name=catalog_promotion_removal_failed
            SYLIUS_MESSENGER_TRANSPORT_PAYMENT_REQUEST_DSN: sync://
            SYLIUS_MESSENGER_TRANSPORT_PAYMENT_REQUEST_FAILED_DSN: sync://
            PHP_DATE_TIMEZONE: ${PHP_DATE_TIMEZONE:-UTC}
            XDEBUG_MODE: ${XDEBUG_MODE:-off}
            XDEBUG_CONFIG: >-
                client_host=host.docker.internal
                client_port=9003
            # This should correspond to the server declared in PHPStorm `Preferences | Languages & Frameworks | PHP | Servers`
            # Then PHPStorm will use the corresponding path mappings
            PHP_IDE_CONFIG: serverName=sylius
        extra_hosts:
            - "host.docker.internal:host-gateway"
        volumes:
          - .:/srv/sylius:rw,cached
          # if you develop on Linux, you may use a bind-mounted host directory instead
#          - ./var:/srv/sylius/var:rw
          - ./public:/srv/sylius/public:rw,delegated
          # if you develop on Linux, you may use a bind-mounted host directory instead
#          - ./public/media:/srv/sylius/public/media:rw
          - public-media:/srv/sylius/public/media:rw
    mysql:
        volumes:
            - mysql-data:/var/lib/mysql:rw
        ports:
            - "3306:3306"
    nginx:
        environment:
            WORKING_DIR: /srv/sylius/vendor/sylius/test-application
        volumes:
            - .:/srv/sylius:ro
            # if you develop on Linux, you may use a bind-mounted host directory instead
#            - ./public/media:/srv/sylius/public/media:ro
        ports:
            # Direct, proxy-free bypass. Host :80 belongs to the shared Traefik
            # (docker/proxy/compose.yml) so that several projects can serve on
            # :80 at once, each on its own hostname — binding it here again
            # would defeat the point and collide with every sibling project.
            - "82:80"
        networks:
            - default   # keep the project-internal alias `nginx` -> php
            - web       # shared proxy network
        labels:
            - "traefik.enable=true"
            # Explicit: nginx sits on two networks and Traefik must be told
            # which one to dial, or it may pick the project-internal one.
            - "traefik.docker.network=web"
            - "traefik.http.routers.sylius-slider.rule=Host(`sylius-slider.localhost`)"
            - "traefik.http.routers.sylius-slider.entrypoints=web"
            - "traefik.http.services.sylius-slider.loadbalancer.server.port=80"
    nodejs:
        image: node:${NODE_VERSION:-20}-alpine
        user: ${DOCKER_USER:-1000:1000}
        working_dir: /srv/sylius
        entrypoint: [ "/bin/sh","-c" ]
        command:
            - |
                cd vendor/sylius/test-application
                yarn install
                yarn build
        volumes:
            - .:/srv/sylius:rw,cached
            - ./public:/srv/sylius/public:rw,delegated
    nodejs-watch:
        # Long-lived `encore dev --watch`. Gated behind the "watch" profile so a
        # plain `docker compose up -d` never starts it.
        #
        #   docker compose --profile watch up -d nodejs-watch
        #   docker compose --profile watch logs -f nodejs-watch
        #   docker compose --profile watch rm -sf nodejs-watch
        #
        # The symlink below is load-bearing: yarn classic COPIES the
        # "@vanssa/sylius-slider-plugin": "file:../../../assets" dependency, and
        # the Stimulus controllers resolve through that copy — so a naive
        # `yarn watch` silently ignores every controller edit. Linking the
        # package at the real assets/ tree puts them back under the watcher.
        image: node:${NODE_VERSION:-20}-alpine
        user: ${DOCKER_USER:-1000:1000}
        working_dir: /srv/sylius
        profiles: ["watch"]
        entrypoint: [ "/bin/sh","-c" ]
        environment:
            # Escape hatch: set WATCHPACK_POLLING=true if inotify misses events.
            WATCHPACK_POLLING: ${WATCHPACK_POLLING:-false}
            CHOKIDAR_USEPOLLING: ${WATCHPACK_POLLING:-false}
        command:
            - |
                cd vendor/sylius/test-application
                LINK=node_modules/@vanssa/sylius-slider-plugin
                if [ ! -L "$$LINK" ]; then
                    yarn install --force
                    rm -rf "$$LINK"
                    ln -s /srv/sylius/assets "$$LINK"
                fi
                exec yarn watch
        volumes:
            - .:/srv/sylius:rw,cached
            - ./public:/srv/sylius/public:rw,delegated
    chrome:
        # Headless Chrome for the @javascript Behat leg (behat.yml's chrome
        # session points at http://chrome:9222). Restart it after an asset
        # rebuild — it keeps compiled bundles in memory.
        image: zenika/alpine-chrome:latest
        command:
            - --no-sandbox
            - --disable-dev-shm-usage
            - --remote-debugging-address=0.0.0.0
            - --remote-debugging-port=9222
            - --remote-allow-origins=*
            - --window-size=1600,950
            - --hide-scrollbars
        shm_size: 2gb
        ports:
            - "9222:9222"
    playwright:
        # Playwright runner for tests/e2e (`make e2e`) and the docs-media
        # generators (`make docs-media`). Behind the "e2e" profile, so a plain
        # `docker compose up -d` never starts it.
        #
        #   docker compose --profile e2e up -d playwright
        #   docker compose --profile e2e exec -T playwright npx playwright test
        #   docker compose --profile e2e rm -sf playwright
        #
        # Declarative service + `exec`, never `docker compose run -d`: a `run`
        # with a never-exiting command leaks an orphan container per session
        # (the same failure mode that leaked 22 php-run containers via the old
        # mate MCP config).
        build:
            context: ./docker/playwright
        # network_mode: host is load-bearing, not a convenience: it is what
        # makes `sylius-slider.localhost` resolve inside the container the same
        # way it does on the host, so the request reaches the shared Traefik
        # with the hostname its router matches on. (Linux only; on
        # macOS/Windows swap this for `extra_hosts` + http://host.docker.internal.)
        #
        # Channel resolution used to be the reason too — the FASHION_WEB channel
        # had hostname "localhost", so any other host 404'd the whole shop. Its
        # hostname is NULL now, i.e. the channel matches every domain, which is
        # what lets :82, sylius-slider.localhost and the Behat hosts all work.
        # Reloading Sylius fixtures sets it back — see docs/dev/local-domains.md.
        network_mode: host
        working_dir: /srv/sylius
        user: ${DOCKER_USER:-1000:1000}
        profiles: ["e2e"]
        environment:
            # Through the shared proxy. Set BASE_URL=http://localhost:82 to
            # bypass it (useful when the proxy is down or being debugged).
            BASE_URL: ${BASE_URL:-http://sylius-slider.localhost}
        command: ["sleep", "infinity"]
        volumes:
            - .:/srv/sylius:rw,cached
    mailhog:
        ports:
            - "8025:8025"

volumes:
    mysql-data:
    public-media:

networks:
    # Created by the shared proxy (`make proxy-up`), never by this project —
    # `external` keeps compose from creating a second, differently-named one.
    web:
        external: true
        name: web
