# Impact report: {{TASK_ID}}

<!-- What this change reaches. Written before the plan, so the plan can be
     scoped honestly. -->

## Direct callers

<!-- file:line for each. Include the ones grep does not find: service ids, event
     names, template names, dynamic dispatch, reflection, configuration. -->

## Data touched

<!-- Tables, columns, cached keys, serialized payloads, files, queues. -->

## Contracts affected

<!-- HTTP API, events, message payloads, exported files, anything another system
     reads. State whether the change is backward compatible. -->

## Other environments

<!-- Does this behave differently in staging, in a different channel, tenant,
     locale, or under a feature flag? -->

## Blast radius if wrong

<!-- What a user sees, what money does, what data becomes inconsistent. -->

## Explicitly unaffected

<!-- What must keep working exactly as it does, and how that will be verified. -->

## Risk tier

<!-- T0…T5 and the trigger that decided it. -->
