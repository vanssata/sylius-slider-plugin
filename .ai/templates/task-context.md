# Task context: {{TASK_ID}}

<!-- Produced by ai-context. This is what every later agent reads instead of the
     repository. Keep it under two pages; if it does not fit, split the task. -->

task:
affected_modules:
entry_points:
execution_flow:
relevant_files:
interfaces:
dependencies:
legacy_constraints:
business_rules:
tests:
known_risks:
unknowns:
open_questions:
